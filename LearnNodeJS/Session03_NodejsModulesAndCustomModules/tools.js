var tools = {
    add: function (x, y) {
        return x + y;
    },

    sayHello: function () {
        return 'Hello Tools';
    }
}

// exports.tools = tools;
// tools.tools.add()

module.exports = tools;
